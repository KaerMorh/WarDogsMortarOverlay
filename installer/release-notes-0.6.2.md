修复更新缓存并禁止双开。

- 同一 Windows 会话只运行一个软件实例，再次打开会唤回已有悬浮窗。
- 更新区域使用独立底色、边框和分隔线，与下方快捷键菜单明显分开。
- 下载完成后关闭软件，下一次启动可离线恢复并校验安装包，继续重启安装，无需重新下载。
- 旧版遗留包在联网验证签名和哈希后可复用；旧包、损坏包、未完成下载会清理，日志及设置备份保留。
- 更新安装成功后清理安装包；清理被文件占用阻止时，下次启动再尝试。

更新到 0.6.2 后生效。下载过程中退出仍需重新下载，暂不支持断点续传。
