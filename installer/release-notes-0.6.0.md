软件更新现在可以直接在设置顶部完成。

- 启动时后台检查正式版更新，可在设置中关闭或手动检查。
- 有新版本时，HUD、大地图和小球右键菜单的“设置”入口显示小红点。
- 支持下载进度、取消下载、失败重试，以及下载完成后点击“重启并安装”。
- 更新清单采用 RSA 签名，安装包校验 SHA-256 和大小；校验失败不会运行。
- 升级保留 UserData，并在安装前备份 settings.json；不会主动重启电脑。

0.5.0 用户需要手动安装本版一次，之后可以直接在软件设置中更新。便携版用户也请先安装安装版。首次安装缺少 WebView2 的电脑需要联网安装微软运行时。
