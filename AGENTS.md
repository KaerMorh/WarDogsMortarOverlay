# 项目开发上下文

继续联机工作前先阅读：

1. `docs/联机开发方案.md`：用户已确认的需求、Go 架构、最新界面方向。
2. `docs/联机协议.md`：Go / C# 消息契约。
3. `docs/联机开发进度.md`：已完成、验证结果、后续工作。

当前联机开发使用 `codex/multiplayer`，从 main e1e66f9 开始。用户要求开发不放在 main 推送。常规实现和本地验证已经授权；不要自动推送、合并 main 或部署到公网。

2026-09-08 最新界面要求：新增任务选择模块是独立、可拖动窗口，原 HUD 不做大改；目前仅保证基本功能，界面精细化后续再做。

用户随后明确要求停止 UI 修改，记录待办并转向 Go 服务端。见 `docs/联机界面待办.md`；在用户重新要求前，不继续修改 UI。

`build.ps1` 发布到稳定版 `app/`。联机开发使用 `build-multiplayer.ps1`，输出 `artifacts/multiplayer/`。本地工具和缓存留在 `.tools/`。UserData/、身份文件和日志不进入发行包或提交。

持续更新进度文档，明确哪些已经测试、哪些只做了规划。保留用户原有未跟踪文件，不将其顺手加入提交。
