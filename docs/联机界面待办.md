# 联机界面待办

2026-09-08 用户要求：停止继续修改 UI，先集中开发 Go 服务端。

同日后续范围更新：用户重新要求加入按钮的状态与防重复操作、去掉快捷键/外观里的重复联机房间设置，只保留独立联机标签，并确保配置记忆。名字显示也按最新身份资料同步。已整理到 `联机测试房与客户端执行方案.md`，目前仅形成方案；其余 UI 待办继续暂停。

现状：任务模块已移入独立可拖动窗口，原 HUD 的正常/简化/小球布局维持原样。当前只要求基本功能。

留待后续：

- 窗口样式、布局密度、成员与任务的呈现方式统一设计。
- 任务选择快捷键的操作体验与系统快捷键冲突处理。
- 侦察模式的精简程度；窗口位置/大小是否记忆。
- 最新一次设置面板同步修改尚需最终 UI 回归；现在暂停 UI 工作，不继续调整。

服务端工作不依赖这些调整。后续对话不要因 UI 待办自动恢复界面修改，先遵循用户最新任务范围。
