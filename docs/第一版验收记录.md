# 第一版验收记录

日期：2026-09-07。

- Release 构建成功，0 编译警告、0 错误。
- 核心测试：256 项断言通过（解析、状态转换、射表节点、射程等）。
- 与固定提交上游 JavaScript 对照：442 个距离、1326 个弹道分支比较全部一致。
- 应用内集成验证：WebView2、本地两图瓦片、地图到 C# 到 HUD、C# 到地图、垃圾尾缀解析、切图历史保护、超程清空 MIL、热键重复拦截、HUD 隐藏恢复均通过。
- 实际渲染预览见 artifacts/preview。主窗口图由 WPF 实际视觉树与 WebView2 CapturePreview 图合成；HUD 图由实际 WPF 视觉树渲染。图中为明确标记的演示坐标。
- Computer Use 窗口自动化工具因 kernel assets 路径错误无法初始化，未完成外部鼠标键盘自动化验收。
- 尚未进行游戏内剪贴板流程、鼠标焦点、反作弊或多屏硬件实测。
- 地图离线瓦片包含 0–4 级；未捆绑 5–7 级高清数据。
- 发布目录 app 在生成验证缓存后约 175 MB；SDK/工具缓存约 1.44 GB（后续可能增长）。

启动：app/WarDogsOverlay.exe。运行时配置位于 app/UserData，开发环境位于 P 盘项目 .tools。多人房间不在本版实现范围。
