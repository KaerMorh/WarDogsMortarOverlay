# 联机开发进度

更新：2026-09-22 DeepSeek OCR 首版作为 v0.6.9 正式发布。功能默认关闭，支持 Ctrl+1 识别地图炮位、Ctrl+2 识别地图目标，独立 OCR 设置标签、API 文字与内置图片测试、截图区域、加密本地密钥、12 秒超时、防重复触发和过期结果保护；测试功能在设置页拆为独立卡片，并修复 PasswordBox 无法点击输入。核心 280 项、OCR 15 项、更新 42 项和上游射表 1326 项通过；自包含客户端与安装包构建通过，隔离安装确认版本 0.6.9、程序哈希、三张离线地图和隐私排除正确，卸载通过。完整界面演示在本机 WebView2 启动阶段未完成，因此仍不记为全量 WPF 通过。GitHub Release v0.6.9 已成为 latest，四个附件齐全，线上摘要与本地一致；安装包 SHA-256 为 `c8eaa6b964ee63b3a412cc6d588e9c86a3aa8e9c36f5b931c1460deea151b32f`，发布提交为 `b15e4dc`。既有 v0.6.8 Release 保持不变。

更新：2026-09-17 PZH校准追加可切换的“XY线性补偿”模式。用户可用当前目标A坐标或方位/MIL录入瞄准点，两种表示实时双向换算；再以剪贴板或手动方式加入实际落点B，按A-B计算固定世界坐标补偿，多组样本取平均并显示离散度。HUD对目标T改用T+(A-B)求SPH-2高抛方位/MIL。原倾斜模式及数据继续保留，模式、样本、开关随设置持久化，换炮位沿用原有保留数据并停用逻辑。校准点列表改为深色背景与浅色文字。核心280项、更新42项、上游射表1326项及完整自包含构建通过；WPF自动验收连续两次被既有WebView2地图就绪检查阻断，未把该项记为通过，待界面实看。v0.6.7 已正式发布为 GitHub latest，四个更新附件齐全；安装包本地SHA-256、清单哈希、版本和文件大小均核对一致，发布提交为03144cc。

更新：2026-09-13 服务端上线。用户明确授权后，已将通用地图 ID 和无任意坐标上限的 Go 服务端部署至 `/opt/wardogs/bin/wardogs-server` 并重启 `wardogs.service`。线上 SHA-256 为 `fc82a67a4d9b5f137267d65bcebf4a2034cf6864aa6ee9c184359f381a1e9e39`，原二进制备份为 `wardogs-server.bak-20260913`。部署前、上传后、备份后、替换后和最终磁盘检查均为 58%，没有超过 95% 限制。服务与测试机器人均 active，服务重启次数为 0；本机回环与公网 `/healthz` 返回 protocol 3，`/readyz` 返回 ready。公网 WSS 冒烟覆盖 Bakurani、Zestafona 和服务端从未预置的 `futuremap2026`：三者均通过双人入房、任务广播、解算状态、资料更新和同步；未来地图使用 `1e12` 坐标验证无旧上限。`testzz4z` 双机器人验收通过。此轮仅更新服务端，不覆盖已发布的 0.6.5 客户端安装包。

更新：2026-09-13 后续架构修正。用户指出服务端不应因新增地图而更新，也不应设任意数值上限。已将 Go 服务端地图校验改为通用安全 ID 格式，坐标仅要求有限数值，不再列举地图名或设置地图坐标边界；8KiB 上行消息大小限制照旧。服务端及客户端对大数值坐标比较避免量化溢出。客户端对未安装地图的房间任务可安全保留并提示，不允许选中解算。Go 全量测试与 vet、未来地图 WebSocket 入房/发布测试、C# 联机及服务重启测试、核心 271 项、更新 42 项、上游射表 1326 项均通过；客户端界面上轮 58 项验证通过，此轮只改数值匹配逻辑并已通过对应测试。Linux amd64 服务端重新构建，SHA-256 为 `fc82a67a4d9b5f137267d65bcebf4a2034cf6864aa6ee9c184359f381a1e9e39`。以上为上线前的本地验证记录。

更新：2026-09-13。0.6.5 增加 Zestafona 地图数据、0–4 级共 341 张离线瓦片与 3 个 Tower 标记，客户端三图切换和各图独立坐标已接通；右键菜单的“隐藏 HUD”加深红色粗体与浅红底色。客户端和 Go 服务端的地图校验均已扩展，但服务器未部署；当前公网服务端仍会拒绝 Zestafona 联机消息。已通过核心 271 项断言、更新 42 项断言、上游射表 1326 项比对、桌面 WPF/WebView2 56 项、Go 全量测试与 vet、C# 联机及服务重启测试。0.6.5 安装包与签名更新清单已正式发布至 GitHub Release；四个附件齐全，清单签名、安装包 SHA-256、线上 latest 更新入口均验证通过，发行载荷含新图 341 张瓦片且不含 UserData。源码发布提交为 `de58f38`，位于 `codex/multiplayer`，没有合并 main。

更新：2026-09-11。为 0.6.4 正式发布补充新版坐标格式兼容：`x98.62, y109.57` 等 X/Y 之间带中英文标点的文本可正常识别，同时保留小数逗号、轴顺序互换和紧邻标签支持。设置第一页新增默认关闭的“开启测试功能”，仅开启后显示联机设置与相关菜单入口，自动入房也受该开关控制。C# 客户端已补齐协议 v3 的 weapon、declined 与客户端本地解算归属模型，修复其此前仍以 v2 接入、会被已部署 v3 服务拒绝的问题。已完成核心解析器 270 项断言、WPF/WebView2 全量界面回归、Go 全量测试、42 项更新测试、1326 项上游射表比对、C# 联机互通与服务重启测试，公网 WSS v3 冒烟亦通过，并生成联机客户端验证包；正式发布验收待本轮继续补记。

更新：2026-09-08。分支 codex/multiplayer；不推送、不合并 main。

2026-09-08 部署补充：已通过本机专用 SSH 密钥在用户的 Ubuntu 22.04 服务器完成部署。固定部署根目录为 `/opt/wardogs`，systemd 服务名为 `wardogs.service`，公网地址为 `wss://wardogs.kaermorh.cloud/ws`。服务以独立低权限用户运行并只监听 `127.0.0.1:8080`，HTTPS/WSS 复用现有 1Panel OpenResty。连接参数和服务器细节保存在本机 `.tools/ssh/服务器连接与操作规则.md`，后续操作前必读。用户要求持续关注服务器磁盘使用率，超过 95% 立即停止操作并告知；本次操作期间从 62% 降至并最终稳定在 57%。

2026-09-08 协议 v3 部署补充：接管中确认 Claude 已上传并启动 v3 服务端及测试机器人，但当时构建的 `/healthz` 仍硬编码报告协议 2。已将健康接口改为读取 `room.Protocol`，重新执行 Go vet、全量 Go 测试和 Linux 静态构建，仅原子替换服务端并重启；公网 WSS v3 完整冒烟与 testzz4z 双机器人验收通过。最终磁盘使用率仍为 57%。

2026-09-08 本轮已完成 `联机测试房与客户端执行方案.md`：连接操作合并与状态提示、设置记忆、最新名字、主动快照同步、协议 v2、跨连接入房限流和 testzz4z 双机器人均已实现、自动验证并部署。限定范围外的 UI 精细化仍暂停，见 `联机界面待办.md`。

## 当前状态

- Go 服务端已可运行：房间、成员/UID 会话替换、重名标签、任务发布、炮位和目标共享、解算者归属、三分钟离线保留、空房销毁。
- 协议 v3，WebSocket /ws；任务是否解算由各客户端根据成员公开的 target/solved 状态在本地判定，服务端不再保存 solvedBy；成员资料新增 weapon，快照含最新身份表并支持同会话 `sync`。旧协议客户端会收到协议不兼容提示，必须升级。
- 已加入连接/消息/速率/队列字节预算、心跳超时、健康检查、显式 WebSocket 停机清理、配置校验与聚合日志。
- 广播仅编码一次，只向同房间发送；未变化状态仅 ACK。避免每次连接/资料变更重发全房所有成员。
- Windows 可执行文件和 Linux amd64 静态二进制已构建；Dockerfile、Compose、可选 Caddy HTTPS 配置已提供。
- Linux amd64 服务端已更新到 `/opt/wardogs/bin/wardogs-server`，当前 SHA-256 为 `fc82a67a4d9b5f137267d65bcebf4a2034cf6864aa6ee9c184359f381a1e9e39`；`wardogs.service` 已启用开机自启，`/healthz` 正确报告 protocol 3。
- `/opt/wardogs/bin/wardogs-testbots` SHA-256 为 `f8babe4a79606c84953319dceaa2ffb1e2e5099c1aaece7d755e8c9686952b06`；`wardogs-testbots.service` 以低权限用户运行、64MiB 内存限制、开机自启，两个固定 UID 常驻 `testzz4z`。
- `wardogs.kaermorh.cloud` 的 Let's Encrypt 证书已签发，有效期至 2026-12-07；Certbot 定时器和 OpenResty 证书部署钩子已启用。
- C# 网络层已与真实 Go 服务互通；加入流程统一为单状态机，包含重复合并、10 秒总超时、成功显示延迟 1 秒、失败至少 3 秒、退出取消、2 秒连接间隔和断线退避重连。原 HUD 保持布局，任务窗口已独立。
- 当前工作均在 codex/multiplayer 工作区，尚未推送或合并 main。用户原有未跟踪文件保持原状。

## 已运行验证

- 2026-09-17 按用户授权恢复PZH相关UI开发并实现倾斜补偿第一版：SPH-2 HUD信息栏提供校准入口，独立窗口支持补偿开关、当前原始高抛解、智能剪贴板/手动落点、增删重置、显式保存和关闭保存确认；炮位变化保留数据并标记需重置，目标行和主读数显示补偿后的高抛方位/MIL。补偿只在本地生效，未修改联机协议或服务端。核心275断言、完整build-multiplayer流程和WPF回归通过；自包含客户端已更新至artifacts/multiplayer/client。尚待游戏内实射验收，未部署。

- Go 配置、房间状态和 WebSocket 测试通过：覆盖会话替换、去重、解算匹配、房间隔离、三分钟边界、容量、限流、入房超时、无心跳、超大帧、跨站 Origin、队列溢出/回收及停机。
- 最终服务端本机负载：100 人分 10 个房间，峰值工作集 24.29MiB，ACK P95 2.86ms，全部连接保持在线。完整范围与限制见联机服务端验收。
- Go vet 通过；build-server.ps1 -Linux 执行验证与静态交叉编译。
- 真实 C# / Go 互通通过，包含重复名、任务 ACK、解算者标记、旧 UID 客户端停止重连、重进任务清空。
- 协议 v2 互通增加改名 identity、UID 解题者、同 sessionId 主动 sync 快照验证；服务端单测覆盖 32 个解题者上限、身份回收、sync 2 秒限频和跨连接来源 IP 限流。
- 真实服务进程终止/重启验证通过：客户端发现断线、拒绝离线发布、自动重连、新房间和会话 ID、不重放旧任务。
- WPF 联机验证通过 29 项，包括重复加入合并、成功延后 1 秒、失败保持 3 秒且不丢当前连接、主动同步、快速退出再加入保留最新意图，以及原有共享、独立窗口和历史回归。
- 原有核心 267 断言、更新系统 42 断言、上游射表 1326 比较通过。
- 原有 WPF/WebView2 回归 44 项通过。主线小球早已为 20px，但旧验证断言仍为 40px；已核实主线源码并仅修正断言，未改小球布局。
- 设置中的重复联机面板已移除；独立联机标签的文本编辑实时合并保存，加入与卸载前强制保存，按钮显示加入中/已加入/正在重连/切换房间，并提供主动同步。已完成自动 WPF 回归，尚待真实游戏中的人工观感验收。
- 用户重新授权调整正常与简化 HUD：设炮位按钮仅在等待复制时使用黄色背景，无炮位和已有炮位均保持普通按钮样式，不使用状态边框；设炮位和选目标均可双击展开坐标粘贴输入，支持回车或按钮确认。目标恢复原有整行常驻样式，已设置的炮位在目标下方独占一行显示。已加入 WPF 自动验证，真实游戏内双击手感仍待人工验收。
- 真实服务器部署验收通过：公网 HTTPS 健康检查、WSS `101 Switching Protocols`、SSH 隧道上的真实 C# 双客户端全协议验收、systemd 重启自恢复、证书续期 dry-run、OpenResty 配置检查均成功。8080 仅监听回环；服务重启后内存约 2.1MiB，受 256MiB 硬限制。
- 本轮公网 WSS 冒烟覆盖两名普通客户端的重名、任务、UID solvedBy、改名和 sync，并验证 testzz4z 的 `+1,+1` 侦察任务与 `+2,+2` 炮位/模拟 solved；机器人事件未形成循环。部署前、上传后、替换后及最终检查磁盘均为 57%。
- 协议 v3 公网 WSS 冒烟覆盖双客户端加入、重名、任务广播、target/solved 转发、weapon/profile 更新、identity、sync 保持会话，以及 testzz4z 双机器人完整行为；服务端和机器人服务均 active，重启次数为 0，最终内存约 2.2MiB/4.9MiB，最终磁盘使用率 57%。
- 部署期间确认既有 SillyTavern 与 1Panel OpenResty 容器保持运行，既有 `uspan.kaermorh.cloud` 站点仍返回原有 401。

## 构建与启动

只开发服务端：`./build-server.ps1`；Linux 交叉编译：`./build-server.ps1 -Linux`。

输出：`artifacts/multiplayer/wardogs-server.exe`、`wardogs-testbots.exe`、`artifacts/multiplayer/linux/wardogs-server`、`wardogs-testbots`。

默认监听 127.0.0.1:8080；客户端地址 ws://127.0.0.1:8080/ws。`wardogs-server healthcheck` 检查 /readyz。服务器未启动时命令会返回非零，这是预期；启动有短暂准备过程，Docker healthcheck 提供 5 秒 start-period。

完整客户端构建脚本为 build-multiplayer.ps1，输出 artifacts/multiplayer/client，不覆盖稳定 app/。

## 2026-09-21：PZH QTE 本地离线识别验证

- 按用户要求，为 `test/` 三张原始截图文件名增加从左到右的 QTE 方向；保留原始图片内容。
- 新增独立 `qte-tests/` 原型，以亮色连通域和四方向几何模板识别箭头，不接入主程序 UI 或按键功能。
- 已实际运行 29 项离线测试，全部通过：15 项原图/缩放正例、9 项真实背景反例、3 项缺箭头反例、2 项纯色反例。
- 实际游戏实时捕获、右箭头真实样本、成功/失败/等待重置反馈、自动按键均未测试。自动装弹接入仍为计划。
- 完整算法、测试方法和复现命令见 `PZH-QTE识别方案与测试.md`。本次没有服务器操作、部署或推送。
- 随后按用户要求编写 `PZH自动装弹-完整编程方案.md`，供后续模型实现：包含模块接口、配置/UI、客户区与 DPI、键盘适配、状态机、真实性反馈门槛、性能测量和验收矩阵。该文档仅为开发方案，未新增实时自动装弹实现，也未新增性能实测结果。

## 后续工作与限制

2026-09-22 PZH 自动装弹按实机反馈调整：设置中的测试功能拆为互不依赖的“显示连接功能”和“自动装弹”，自动装弹勾选后立即启用并展开设置；HUD 提示仅在自动装弹已开启且为 SPH-2 时显示。连续同方向 QTE 不再比较方向串，也不要求采样到可能已经错过的绿色/红色中间帧；当前画面重新稳定识别出四个白色箭头即可进入下一组。运行中再次按换弹键会取消并释放旧任务，然后立即从本次前台窗口开始新流程。新增三项按键变绿图片测试，QTE 图片测试 47/47、WPF 自动界面验收与客户端 Release 构建均通过；原 `adjusted-client/` 被运行中的程序占用，本次自包含测试包输出至 `artifacts/pzh-reload/adjusted-client-v2/`。真实游戏内打断、颜色反馈和连续同 QTE 尚待交互验收。本轮未操作服务器、未部署、未推送。

2026-09-21 PZH 自动装弹首版已接入本地客户端：设置实验区、游戏进程绑定与客户区框选、共用 QTE 识别器、低级键盘触发、SendInput 方向输入、可取消的四组上限流程和状态提示。未改 Go/联机协议，未操作服务器，未部署或推送，未覆盖 `app/`。原 29 项离线测试及 15 项小区域对照均通过（44/44）；旧设置兼容、新设置 JSON 往返与无效区域归一化的小程序检查通过；纯识别 230×60 基准 200 次平均 3.30 ms、P95 4.14 ms。自包含 win-x64 客户端位于 `artifacts/pzh-reload/client/`。命令会话无交互式桌面，截图基准返回句柄无效；游戏实时捕获、键盘输入、完整重启保留和连续四组行为均未实测。详见 `PZH-QTE识别方案与测试.md`。

2026-09-21 用户反馈要求去掉游戏窗口绑定与切换倒计时：现已改为设置中的“测试识别”和“框选识别区域”先待命，用户在游戏里按换弹键时直接以当时前台窗口启动；测试只截图识别，不发送按键。主计算 HUD 的正常与简化界面新增自动装弹开关、执行状态、最近识别方向提示。因没有预绑定进程，用户须保证按键时游戏处于前台；任务内仍固定并检查本次窗口句柄与 PID。新自包含构建在 `artifacts/pzh-reload/interaction-client/`；旧 `client/` 被运行中的进程占用，未覆盖。交互式游戏验收仍未完成。

2026-09-21 用户要求换炮位后直接重置 PZH 倾斜校准。现在当前地图/炮位变化时自动将校准绑定到新炮位，清空旧倾斜与 XY 样本、拟合结果和启用状态；打开中的校准窗口同步新草稿、清除旧输入及等待中的落点捕获，可直接录入新样本。相同炮位重复设置不重置。旧 `NeedsReset` 设置在已有炮位时也会归一化为新起点。已运行本地重置模型检查、核心 280 项断言和 Release 构建；真实桌面校准窗口流程尚未验收。本轮不改联机协议或服务端，不部署、不推送。

- 公网已部署，但尚未做长时间浸泡和真实游戏实机多人验收；本次没有在服务器运行高并发负载，不能把本机负载结果当作服务器容量承诺。
- 机器人首版的 solved 是协议交互模拟，不是射表正确性验收；动作队列最多 64 条、任务去重缓存最多 4096 条，越界偏移会跳过并记录。
- 未运行 Go race detector（本机没有所需 C 编译工具链）；并发逻辑已有普通测试。
- 本地负载是 Windows 同机回环测试；不能作为 2 核 2G Linux 服务端容量承诺。负载明细见 `联机服务端验收.md`。
- NuGet 在线漏洞数据请求出现 NU1900，重试后仍不可达；构建成功不代表在线漏洞审计通过。
- UI 精细化与游戏实机快捷键/剪贴板验收留待后续，不自动恢复 UI 修改。

## 代码入口

- WPF .NET 10，Controller.cs 管理配置/剪贴板/历史，Core.cs 是本地 Session 和解算。
- server/internal/room：消息模型与房间状态；internal/transport：连接、队列、速率、心跳、停机；internal/config：部署参数。
- server/cmd/wardogs-server：启动入口、健康检查命令、清理计时器和聚合统计。
- server/cmd/wardogs-testbots：testzz4z 双机器人；server/cmd/wardogs-smoketest：可重复运行的公网协议与机器人验收工具。
- src/Multiplayer：协议、持久 UID、网络客户端、房间状态和桌面适配。任务选择窗口 TaskPickerWindow.cs 已暂停 UI 修改。
- History 原先无限长，本次已统一限制为 100 条；历史本来就不落盘。

## 2026-09-22：DeepSeek OCR 首版方案（仅设计）

用户暂缓更新源工作，确认外部模型识别测试可行，要求先编写交给后续模型的实现方案。已新增 docs/DeepSeek-OCR首版编程方案.md，明确 Ctrl+1/2、独立测试功能开关、两处快捷键同步、文字与内置图片 API 自测、消息栏状态、防抖/超时/过期结果保护和最小实现约束。本轮未修改客户端代码、未读取密钥内容、未调用付费 API、未构建或进行游戏实测；无服务器操作、部署、提交或推送。

## 2026-09-22：DeepSeek OCR 首版实现

已按方案实现客户端 DeepSeek OCR：独立测试功能开关、DPAPI 密钥保护、默认 Ctrl+1/2、普通快捷键组与 OCR 面板同步编辑、中央地图截图/区域校准、官方 deepseek-flash JSON 请求、严格结果校验、12 秒超时、忙碌防重、500ms 冷却、输入变化取消，以及 HUD/主窗口独立 `[OCR]` 状态。内置测试按钮顺序验证文字和裁剪地图图像，不改变坐标；成功识别通过现有 Session 设置炮位或目标，来源为 DeepSeek OCR。

CoreTests 280 项、OCR 离线 15 项（含异常 JSON 与 DPAPI 无明文检查）通过。生产请求代码真实调用文字和图片各一次并得到预期 (12.34,56.78)、(99.64,112.28)；另有两次等价请求契约探测。Release 构建及 win-x64 自包含发布通过，测试包为 `artifacts/multiplayer/ocr-client/`，不含 UserData 或开发密钥。WPF OCR 设置三项自动检查通过，后续旧流程因当前会话 WebView2 地图未就绪而中止。真实游戏截图、不同 DPI/多显示器、全局快捷键、防抖/超时和迟到结果的交互验收仍待用户实机测试。本轮未操作服务器、未部署、未提交或推送，也未覆盖稳定版 app/。

用户实测反馈测试功能排布疲劳、OCR 应与联机一样使用独立标签，并且 API Key 无法输入。现已将联机、PZH 自动装弹、DeepSeek OCR 分成带边框和说明的独立卡片；OCR 详细配置移到 HUD 设置中的 `DeepSeek OCR` 独立标签，通用页只保留开关和入口；标签栏改为自动换行。API 输入问题确认为 HUD 拖动命中判断遗漏 PasswordBox，现已把 PasswordBox 作为输入控件处理。自动界面检查确认卡片、独立标签、启用后的 API 输入框和输入焦点命中均通过；后续旧流程仍停在当前环境 WebView2 地图未就绪。

原 `artifacts/multiplayer/ocr-client/` 正由进程运行并锁定 DLL，本次未关闭用户进程或覆盖旧包；调整后的自包含版本输出到 `artifacts/multiplayer/ocr-client-v2/`。Release 构建和发布通过，仍有既有 NU1900 在线漏洞数据不可达警告。
