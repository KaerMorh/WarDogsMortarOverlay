# Go 联机服务端验收（2026-09-08）

## 交付范围

Go net/http + coder/websocket 服务，协议 v1；实现房间与成员状态、任务发布/去重、目标与炮位共享、解算归属、三分钟离线清理和同 UID 会话替换。

本轮按用户最新指示只推进服务端；UI 调整已暂停，见联机界面待办。

## 自动化验证

`go vet ./...`、`go test ./... -count=1` 通过。覆盖：

- 配置默认值、合法覆盖、错误配置拒绝。
- 重名标签、同 UID 替换、旧会话写入和旧断开回调隔离。
- 每人三条任务、重复 taskId 去重、发布不改目标、跨地图不误标解算、曾解算记录保留。
- 房间隔离、不变状态只 ACK、房间容量、三分钟精确边界、并发房间发布、旧快照不可变。
- WebSocket 真实入房和发布、入房超时释放名额、无效坐标拒绝、跨站 Origin 拒绝、连接容量。
- 消息限流、超大帧、心跳超时、发送队列条数及字节预算、停机关闭连接和释放排队内存。

真实 C# ClientWebSocket → Go 二进制验证通过：入房、任务 ACK/广播、解算者归属、重名显示、身份替换停止互踢重连、离线状态。另实际终止并重启服务进程，验证客户端自动重连、重建空房、新会话 ID、不重放离线发布。

`wardogs-server healthcheck`、GET /healthz、GET /readyz 本机验证通过。首次紧接进程启动时健康检查曾返回连接拒绝；等待服务开始监听后成功，属于启动时序，Docker 已设 5 秒启动宽限。

## 本机负载结果

环境：Windows 本机回环网络，Go 服务与 C# 测试客户端共用开发机。每个用户先发布三个任务，随后每秒两次目标更新，持续约 15 秒。每一档全部连接保持在线。不是公网/长时间浸泡测试，也不是 2 核 2G Linux 服务器的容量保证。

| 同时连接 | 房间安排 | 峰值工作集 MiB | ACK P50 ms | ACK P95 ms | ACK 最大 ms |
|---:|---|---:|---:|---:|---:|
| 10 | 1 房间 × 10 人 | 17.59 | 0.50 | 1.76 | 3.04 |
| 50 | 5 房间 × 10 人 | 20.21 | 0.77 | 2.23 | 2.75 |
| 100 | 10 房间 × 10 人 | 24.29 | 0.93 | 2.86 | 5.59 |
| 32 | 1 房间 × 32 人 | 21.41 | 1.94 | 3.56 | 6.93 |

采集到的单核 CPU 时间比例分别为 0%、6.95%、3.26%、1.12%。短采样且与其他开发进程共机，0% 不代表没有 CPU 开销，各档 CPU 数值不能当作严格线性比较。原始文件在 artifacts/multiplayer/server-load-results.json。

广播优化：同一事件编码一次后共享字节；只更新变化的成员，重复 profile/origin/target 不广播。发送队列按每连接 128 条/1MiB、全服务 32MiB 控制；预算为排队数据上限，不是进程总内存上限。

## 构建产物

- Windows amd64：artifacts/multiplayer/wardogs-server.exe，7,906,304 字节。
- Linux amd64（CGO=0）：artifacts/multiplayer/linux/wardogs-server，7,688,352 字节。
- 服务端单独构建：`./build-server.ps1` 或 `./build-server.ps1 -Linux`。
- 配置与部署：server/README.md、Dockerfile、compose.yaml、compose.public.yaml、Caddyfile。

## 仍未验证

- 没有采用附带的 Docker/Caddy 方案；实际部署使用静态二进制、systemd 和服务器现有的 1Panel OpenResty，因此 Docker 镜像构建与 Caddy 路径仍未验证，但不影响当前线上路径。
- Go race detector 需要的本机 C 工具链未提供；已有普通并发测试，不能代替 race 检测。
- 公网故障、长时间浸泡、服务器高并发负载和游戏实机输入验收仍待进行。

## 真实服务器部署验收（2026-09-08）

- 固定部署根目录：`/opt/wardogs`；程序：`/opt/wardogs/bin/wardogs-server`；systemd：`wardogs.service`。
- 公网地址：`wss://wardogs.kaermorh.cloud/ws`；健康检查：`https://wardogs.kaermorh.cloud/healthz` 和 `/readyz`。
- Linux 产物 SHA-256：`33fc14d54744bd1f58dfb8b2a4b78934e7401564cbd1f1f93e1d17e48bcd185d`，与本地重建产物一致。
- 服务以 `wardogs` 低权限用户运行，仅监听 `127.0.0.1:8080`；systemd `enabled`、`active`，重启后自动恢复；实测空闲内存约 2.1MiB，`MemoryMax=256MiB`。
- 复用 1Panel OpenResty 的 80/443；配置语法检查通过，公网 TLS 健康检查成功，WebSocket 升级返回 101。
- Let's Encrypt 证书覆盖 `wardogs.kaermorh.cloud`，有效期至 2026-12-07。Certbot 定时器启用，`certbot renew --dry-run` 成功，续期部署钩子位于 `/opt/wardogs/deploy/deploy-certificate.sh`。
- 真实 C# ClientWebSocket 双客户端通过 SSH 隧道连接线上 Go 进程，验证入房、重名标签、任务广播与 ACK、taskId 去重、解算者归属、同 UID 替换和离线状态。当前 Windows 沙箱将公网域名解析到代理保留地址 `198.18.0.66` 且 TLS 凭据不可用，因此协议验收走 SSH 隧道；公网 TLS/WSS 升级另在服务器侧独立验证。
- 操作前后持续检查根分区（同时承载 `/opt` 与 Docker 数据）；最高观测 62%，最终 57%，未触发 95% 停止线。
- 既有 SillyTavern、1Panel OpenResty 容器保持运行，既有站点仍返回原有 401。

## 本地启动

在项目根目录执行（Windows）：

```powershell
$env:WARDOGS_ADDR='127.0.0.1:8080'
./artifacts/multiplayer/wardogs-server.exe
```

客户端服务地址使用 ws://127.0.0.1:8080/ws。使用任意符合规则的房间码，例如 aabb，两个客户端相同码进入同房。正式公网客户端填写 `wss://wardogs.kaermorh.cloud/ws`；部署和维护说明在 server/README.md。

## 后续对话接续

先读 AGENTS.md、联机开发进度、联机开发方案、联机协议。不要自动恢复 UI 修改。服务已部署，后续优先做长期稳定性、服务器容量和游戏实机验收；未来线上更新仍需用户明确授权，不要自行部署。
