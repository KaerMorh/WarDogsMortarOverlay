# v0.2 体验修订验收

- 控制面板：348×222 DIP；透明数字：314×74 DIP；圆球：40×40 DIP（默认缩放）。
- HUD 内提供三模式、两图、两炮种切换，以及炮位、目标、暂停操作。
- 快捷键录入、清除、保存和外观调整在 HUD 齿轮内；主程序设置另设分页。
- 数字/标题/空白采用鼠标捕获拖动并换算 DPI，按钮和输入控件不触发拖动。
- 两图分别显示 5 / 4 个 Tower；切图直接恢复坐标，已移除历史确认。
- 255 项核心断言，442 个距离 / 1326 次上游射表分支比对通过。
- 原生 WPF / WebView 集成检查通过，包括双向坐标同步、Tower、形态尺寸和隐藏恢复。
- Verification 内图片为应用实际渲染。尚未完成真实游戏内鼠标、快捷键和反作弊兼容性验收。
